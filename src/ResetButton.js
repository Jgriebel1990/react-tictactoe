import React, { Component } from 'react';

class ResetButton extends Component {
    render(){
        return(
            <button onClick={this.props.reset}>dookie</button>
        );
    }
}

export default ResetButton;